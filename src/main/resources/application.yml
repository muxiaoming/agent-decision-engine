spring:
  application:
    name: springai-langfuse3-demo
  profiles:
    active: local,dev
  mvc:
    favicon:
      enabled: false       # 禁用 favicon.ico 请求，避免 500 错误噪音
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY:your-deepseek-api-key}
      chat:
        model: deepseek-chat
      base-url: https://api.deepseek.com
    openai:
      api-key: ${MIMO_API_KEY:your-mimo-api-key}
      base-url: https://token-plan-cn.xiaomimimo.com
      chat:
        options:
          model: xiaomi/mimo-v2.5
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:your-dashscope-api-key}
      chat:
        options:
          model: qwen-plus-2025-01-25
    # Spring AI 观测配置（Langfuse 官方推荐）
    chat:
      observations:
        log-prompt: true       # 在 Trace 中记录提示词内容
        log-completion: true   # 在 Trace 中记录补全内容

server:
  port: 8182

# OpenTelemetry + Micrometer 观测配置（Langfuse 官方推荐）
management:
  observations:
    annotations:
      enabled: true       # Enable @Observed 注解

# OTel 配置
otel:
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://localhost:3000/api/public/otel
      headers:
        Authorization: "Basic cGstbGYtNjkxNDA3NzItMzgzMi00ZWU1LWExOTEtNWU5MWViNTdjNjEwOnNrLWxmLWI2ZDFmOTQ2LTMxOWEtNDE1Yy04MzM1LTI4N2JhNmY3YjRlYQ=="
        x-langfuse-ingestion-version: "4"
